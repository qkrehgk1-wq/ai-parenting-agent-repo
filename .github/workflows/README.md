# GitHub Workflows Plan

이 폴더는 나중에 GitHub Actions를 붙이기 위한 자리다.

초기 권장 자동화는 아래와 같다.

1. Issue 생성 시 기본 라벨 검증
2. PR 생성 시 체크리스트 누락 여부 검사
3. `risk:medical` PR은 수동 승인 전 머지 차단
4. 주간 KPI 리포트 초안 생성

현재 단계에서는 운영 규칙과 템플릿만 먼저 정의한다.
